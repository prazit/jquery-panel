	
	/* 1.remove the end and the begin of plugins that look like this
	})(jQuery);
	... 
	(function($,undefined){
	...
	'use strict';
	*/
	// regex: (\}\)\(jQuery\);\s*\R)(^((?!'use strict';).)*$\R)*\s*'use strict';
	
	/* 2.put this on top and replace string by variable, open notepad++ open find dialogue then move it to bottom right of document and change search mode from regular expression to normal and then select 'xxx' and press Ctrl+Alt+Shift+F */
	/* issued: 1.search mode must not be regular expression, 2.make sure to run woking-mode-batch before use Ctrl+Alt+Shift+F because of it's can make error during automatic search replace process */
	
	var
		cstring = 'string',				// 17
		cfunction = 'function',			// 9
		cobject = 'object',				// 5
		cblank = ' ',					// 15
		cclass = 'class',				// 4
		chidden = 'hidden',				// 13
		c100pc = '100%',				// 18
		cclick = 'click touchstart',	// 13
		un = undefined;					// 154
	
	Appanel.version+=' JPSR85';
	